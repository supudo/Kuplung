language: cpp
os: osx
compiler:
  - clang
  - gcc

before_install:
  - brew update
  - brew install cmake boost sdl2 glm minizip glfw3 assimp protobuf

script:
  - mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Debug ../Kuplung && cmake --build ./
